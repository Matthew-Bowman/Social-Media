function Signup({author, content, timestamp}) {
    return (
      <div>
        <h1>{author}</h1>
        <p>{content}</p>
        <p>{timestamp}</p>
      </div>
    );
  }
  
  export default Signup;
  